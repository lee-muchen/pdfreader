<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muchen.pdfreader">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name = "android.permission.PERMISSIONNAME"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/NoActionBar"></activity>
        <activity android:name=".TxtReadActivity" android:label="文字朗读"></activity>
        <activity android:name=".PDFReadActivity" android:label="本地文件"></activity>
        <activity android:name=".FileInfoActivity" android:theme="@style/NoActionBar"></activity>
        <activity android:name=".PDFToTxtActivity" android:label="PDF格式转换"></activity>
            </application>
    </manifest>